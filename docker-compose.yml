version: '3.8'  # Adicione esta linha para especificar a versão do Compose
services:
  spring-app:
    build: .
    container_name: spring-app
    ports:
      - "8080:8080"
    networks:
      - spring-network

networks:
  spring-network:
    driver: bridge
